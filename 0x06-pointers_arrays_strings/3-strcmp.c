#include "main.h"

/**
 * _strcmp - copies a string into another string while using a third param
 *
 *@s1: A string
 *@s2: A string
 *
 * Return: Returns 0 if strings are equal, >0 if s1 is > s2, <0 if s1 < s2
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
