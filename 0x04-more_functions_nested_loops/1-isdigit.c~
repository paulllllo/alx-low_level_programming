#include "main.h"

/**
 * _isupper -  A function that checks if a int is uppercase
 *
 * @c: An integer arguement
 *
 * Return: Returns 0 or 1
 */

int _isupper(int c)
{
	if (isupper(c) > 0)
		return (1);
	else
		return (0);
}
