#include "main.h"

/**
 * _memcpy - Copies the bytes stored in one array to another array
 *
 *@dest: A pointer to the address of the destination array
 *@src: A pointer to the source array to be copied from
 *@n: Number of bytes to be replaced
 *
 * Return: Returns the pointer 's'.
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	memcpy(dest, src, n);
	return (s);
}
