#include "main.h"

/**
 * swap_int -swaps teo variables
 *
 *@a: An integer pointer
 *@b: An integer pointer
 *
 * Return: Always 0.
 */

void swap_int(int *a, int *b)
{
	int *tmp;

	*tmp = a;
	*a = b;
	*b = tmp;
}
